/// <reference types="vite/client" />

// Browser API globals that are available in the browser environment
declare global {
  // Browser globals are already defined in lib.dom.d.ts
  // We just need to ensure they're available in the global scope
}
